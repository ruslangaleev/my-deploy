name: Build & Test

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET 3.1.x
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.x
    - name: Restore dependencies
      run: dotnet restore ./src/backend/MyProject
    - name: Build
      run: dotnet build ./src/backend/MyProject --no-restore
    - name: Test
      run: dotnet test ./src/backend/MyProject --no-build --verbosity normal
